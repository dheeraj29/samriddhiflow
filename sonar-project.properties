sonar.projectKey=dheeraj29_samriddhiflow
sonar.organization=dheeraj29

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=samriddhiflow
sonar.projectVersion=1.6.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclusions - Ignore generated files, build artifacts, and platform-specific code
sonar.exclusions=**/*.g.dart, \
                 **/*.freezed.dart, \
                 **/*.mocks.dart, \
                 **/*_test.dart, \
                 test/**, \
                 build/**, \
                 .dart_tool/**, \
                 android/**, \
                 ios/**, \
                 web/**, \
                 windows/**, \
                 linux/**, \
                 macos/**, \
                 **/*.png, \
                 **/*.jpg, \
                 **/*.ttf, \
                 **/*.otf, \
                 firebase_options.dart, \
                 **/firebase_options.dart, \
                 firebase.json